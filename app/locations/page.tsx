import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function LocationsPage() {
  return (
    <PlaceholderPage
      eyebrow="Locations"
      title="Location index"
      description="Locations will eventually become structured worldbuilding records with parent-child hierarchies, culture links, timeline references, and book-level relevance."
      placeholderTitle="Locations placeholder"
      placeholderDescription="The initial Firestore setup creates locations/loc_001 inside the default project so route scaffolding and schema inspection can move forward before CRUD screens exist."
    />
  );
}
