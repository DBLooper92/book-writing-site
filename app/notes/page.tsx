import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function NotesPage() {
  return (
    <PlaceholderPage
      eyebrow="Notes"
      title="Notes workspace"
      description="Notes will support ad hoc canon capture, linked research, and developer-facing planning records tied back into the story-bible entities."
      placeholderTitle="Notes placeholder"
      placeholderDescription="The default project seeds notes/note_001 as a visible root note so the project document can point at an inspectable note record from day one."
    />
  );
}
