import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function CharactersPage() {
  return (
    <PlaceholderPage
      eyebrow="Characters"
      title="Character index"
      description="Characters will eventually support rich story-bible records, cross-linking into books, chapters, scenes, factions, locations, timeline events, and notes."
      placeholderTitle="Characters placeholder"
      placeholderDescription="The development initializer creates a starter character document at characters/char_001 so the Firestore structure is visible and linked to the rest of the seed project."
    />
  );
}
