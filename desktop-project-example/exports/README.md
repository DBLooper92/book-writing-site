# Exports

These files are regenerated from SQLite so Codex can browse canon quickly without opening raw structured tables for every task.

Rules:

- prefer reading exports first
- do not treat them as the source of truth
- regenerate them after approved applies

