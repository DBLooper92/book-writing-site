# Attachments

This folder holds user-owned files stored beside the database.

Expected subfolders:

- `images/`
- `documents/`

The database should store relative paths such as:

- `attachments/images/north-gate-market-map.png`
- `attachments/documents/chapter-one-draft.docx`

Do not store absolute machine-specific paths in the database.

