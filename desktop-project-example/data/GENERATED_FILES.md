# Generated Runtime Files

The desktop app should create the actual SQLite database file at runtime.

Expected files:

- `project.sqlite`
- `project.sqlite-wal`
- `project.sqlite-shm`

This example keeps the schema and seeds in text form so the structure is reviewable.
