# Proposals

This directory holds review-first AI outputs.

- `pending/`: newly drafted proposals waiting on user review
- `approved/`: approved proposal files ready for preview or apply
- `rejected/`: rejected or archived proposals

The app UI can surface these records directly, or it can sync them into `ai_sessions` and `ai_proposals` inside SQLite.

